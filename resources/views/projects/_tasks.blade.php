

{{-- @if ($project->tasks)
        @foreach ($project->tasks as $task)
            {{ $task->name }}
        @endforeach
    @else
        <p>No hay tareas</p>
    @endif --}}

    
    {{-- <div id="task-form"></div>

    <div id="tasks-list">

    </div> --}}
